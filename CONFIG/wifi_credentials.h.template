#ifndef __WIFI_CREDENTIALS_H
#define __WIFI_CREDENTIALS_H

/* ============================================================
 * WiFi 凭据与云平台密钥 (模板)
 * ------------------------------------------------------------
 * 使用方法:
 *   1. 复制本文件为 wifi_credentials.h
 *   2. 填写实际值(凭据文件已被 .gitignore 排除, 不会入库)
 * ============================================================ */

#define WIFI_SSID       "your_wifi_ssid"         /* WiFi名称, 不支持中文, 使用2.4GHz */
#define WIFI_PASSWORD   "your_wifi_password"     /* WiFi密码 */
#define WIFI_UID        "your_bemfa_uid"         /* 巴法云用户UID */
#define WIFI_TOPIC_LED  "your_control_topic"     /* 控制主题 */
#define WIFI_TOPIC_DATA "your_data_topic"        /* 数据主题 */

#endif
